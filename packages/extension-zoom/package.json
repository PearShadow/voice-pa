{
    "name": "extension-zoom",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "build": "echo 'Building Zoom Extension...' && mkdir -p dist && cp manifest.json popup.html dist/ && cp -r src dist/",
        "test": "echo 'No tests for extension-zoom yet'"
    }
}